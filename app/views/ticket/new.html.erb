<%= simple_form_for(@ticket, defaults: {label: false,error_html: { class: 'rederror'}}) do |f| %>
  <%= f.input :title,placeholder: "What is a Droplet?",required:true %>
  <%= f.button :submit, "Create Ticket",:class=> "signupbutton" %>
<% end %>
